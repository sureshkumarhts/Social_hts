<div id="active_poll_<%=question%>">

  <%= in_place ?
       form_remote_tag(:url => url_for(:action =>'ap_vote_registered'), :update => "active_poll_#{question}") :
       form_tag(url_for( :action => 'ap_vote_registered' )) -%>

    Question: <i><%= question.description %></i><p/>     
    <p>
    <% answers.each do |answer| %>
      <%= radio_button_tag 'active_poll_answer[]', answer[:id] unless question.multiple -%>
      <%= check_box_tag    'active_poll_answer[]', answer[:id] if question.multiple     -%>
      <%= answer[:description] %>
      <br/>
    <% end %>
    </p>

    <input type="hidden" name="poll_name" value="<%= poll_name -%>"/>
    <input type="hidden" name="redirect" value="<%= redirect -%>"/>
    <input type="hidden" name="in_place" value="<%= in_place -%>"/>
    <%= submit_tag 'Vote' %>

  </form>

</div>
